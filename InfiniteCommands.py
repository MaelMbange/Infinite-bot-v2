"""
@start(gamertag) => start a session for gamertag
    if gamertag is None: user's gamertag is called [if user is registered]

@stop => stop the session

@register => register or modify the user's gamertag to the file

@sayMyName => say the user's gamertag

@global(gamertag) => display the global stats of gamertag
    if gamertag is None: user's gamertag is called [if user is registered]

@lastGame(gamertag) => display the last game stats of gamertag
    if gamertag is None: user's gamertag is called [if user is registered]

@clear => clear the private messages

"""